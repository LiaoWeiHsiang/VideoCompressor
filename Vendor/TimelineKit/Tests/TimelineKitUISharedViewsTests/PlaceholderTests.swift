import XCTest
@testable import TimelineKitUISharedViews

/// Placeholder — real smoke tests land with the coordinator migration.
final class TimelineKitUISharedViewsPlaceholderTests: XCTestCase {
    func testModuleCompiles() {
        XCTAssertTrue(true)
    }
}
